version: 1
frontend:
 phases:
  preBuild:
   commands:
    - nvm use $VERSION_NODE_16
    - npm ci
  build:
   commands:
    - npm run build
 artifacts:
  baseDirectory: dist/tienda
  files:
  - '**/*'
 cache:
  paths:
  - node_modules/**/*